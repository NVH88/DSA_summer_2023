int x, y;

void extendedEuclid(int a, int b) {
    if(b == 0) {
        x = 1; y = 0; return;
    }
    extendedEuclid(b, a % b);
    int tmp = x;
    x = y;
    y = tmp - (a / b) * y;
}

// return x
// (a * x) mod MOD = 1
